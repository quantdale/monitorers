#!/usr/bin/env sh
echo "=== pre-push: running canonical fast verification (frontend + Rust + security) ==="
npm --prefix sys-monitor-tauri run verify:fast
status=$?
if [ $status -ne 0 ]; then
  echo "FAILED: canonical fast verification"
  exit $status
fi
echo "=== pre-push: canonical fast verification passed ==="
