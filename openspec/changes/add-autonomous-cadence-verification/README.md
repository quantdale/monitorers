# add-autonomous-cadence-verification

Headless emit-sink cadence probe + JSONL checker so an AI agent can autonomously verify the 1Hz history / 4Hz liveness invariants against real hardware, closing the manual E2E gate (task 5.5 / 8.7).
